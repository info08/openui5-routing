{
  "name": "crudui5",
  "version": "1.0.0",
  "description": "OpenUI5 Crud Application",
  "scripts": {
    "start": "ui5 serve -o index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ui5 build --all --clean-dest",
    "serve-dist": "ws --compress -d dist --open"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@ui5/cli": "^4.0.9",
    "local-web-server": "^5.4.0"
  }
}
